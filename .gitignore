# AI assistant instruction files — private, not for commit
CLAUDE.md

# Tailored resumes for specific job applications — local only
docs/resumes/applications/

# Standalone tools live outside this repo
/tools/

# Environment and secrets
.env
.env.local
*.key
*secret*
*credential*
*password*
.mcp.json

# Keep example/template credential files
!*credentials.example*
!*credentials.template*

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor files
.vscode/
*.swp
*.swo

# Temp/logs
*.tmp
*.log
.cache/

# SE season-06: local-only "current state from live server" snapshots
# Pulled fresh from the server before each settings change. Never committed.
sturmgrenadier/space-engineers/season-06/OLD SETTINGS/
